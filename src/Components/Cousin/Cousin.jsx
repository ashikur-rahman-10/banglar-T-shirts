import React from "react";

const Cousin = ({ children }) => {
    return (
        <div className="border">
            <h2>Cousin</h2>
            {children}
        </div>
    );
};

export default Cousin;
